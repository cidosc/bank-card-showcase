/**
 * 卡面质感（表面风格）。
 *
 * 对外只有两种，便于理解与切换：
 * - `"flat"`（默认）：轻量化平面风。极轻的投影 + 1px 内描边，去掉霓虹边缘/辉光，
 *   并收平 3D 透视，适合与简洁的浅色电商页面融为一体；
 * - `"physical"`：较强的实体感。保留底层较重的黑色投影与边缘高光，
 *   适合暗色 / 沉浸式场景。
 */
export const BANK_CARD_SURFACES = ["flat", "physical"] as const;
